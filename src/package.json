{
  "name": "demo-chat-app",
  "version": "1.0.0",
  "description": "demo chat app",
  "main": "index.js",
  "scripts": {
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "lint": "eslint . --ext .ts",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "mongoose": "^6.3.3",
    "socket.io": "^4.5.0"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "jasmine": "^3.1.0",
    "request": "^2.87.0"
  },
  "husky": {
    "hooks": {
      "pre-push": "git diff HEAD --quiet && npm run prettier-format && npm run lint"
    }
  }
}
