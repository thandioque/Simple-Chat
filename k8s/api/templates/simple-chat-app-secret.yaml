apiVersion: v1
kind: Secret
metadata:
    name: {{ .Release.Name }}-simple-chat-app-secret
type: Opaque
data:
    mongo-user: {{ .Values.mongo.credentials.userName | b64enc | quote }}                
    serverPort: "{{ .Values.app.serverPort }}"
