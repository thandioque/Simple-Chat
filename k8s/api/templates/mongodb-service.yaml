# The MongoDB Service 
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongo-service
spec:
  selector:
    app: {{ .Release.Name }}-mongodb-deployment
  ports:
  - port: {{ .Values.mongo.portNumber }}  
  type: ClusterIP 