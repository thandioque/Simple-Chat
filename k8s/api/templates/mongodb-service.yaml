# The MongoDB Service 
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongo.name }}
spec:
  selector:
    app: {{ .Values.mongo.name }}
  ports:
  - port: {{ .Values.mongo.portNumber }}  
  type: ClusterIP 